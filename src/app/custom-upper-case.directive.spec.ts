import { CustomUpperCaseDirective } from './custom-upper-case.directive';

describe('CustomUpperCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomUpperCaseDirective();
    expect(directive).toBeTruthy();
  });
});
